# Veritaban? Ba?lant? Ayarlar?
spring.datasource.url=jdbc:mysql://localhost:3306/aktour_db?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
# Kendi MySQL ?ifreni buraya yaz
spring.datasource.password=mirza2026

# JPA / Hibernate Ayarlar?
# Tablolar? koddan otomatik olu?turur (Geli?tirme a?amas?nda 'update' iyidir)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Sunucu Portu (Varsay?lan 8080'dir, de?i?tirmek istersen buray? kullanabilirsin)
server.port=8080